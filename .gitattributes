# UART captures preserve the exact CR-delimited bytes emitted by the firmware.
# Treat them as binary evidence so whitespace checks do not rewrite or reject
# the captured stream.
firmware-validation/evidence/**/uart.log binary
