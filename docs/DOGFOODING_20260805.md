# ドッグフーディング記録: PicoTetris（2026-08-05）

## 目的

このプロジェクトの第一目的は「AIがPicoCalc向けプログラムを自ら観測・検証・修正
できること」である。Milestone 1完了時点で標準templateのスモークは通るようになった
が、**実際にアプリを書いて作り切れるか**は一度も試していなかった。

そこでテトリス風ゲームを1本、**実機を一度も使わずエミュレーターだけで**作った。
目的はゲームそのものではなく、**ワークフローのどこに穴があるかを見つけること**である。

## 結果: 作れた

`app/main.cpp`のみ（約420行）で完成した。BSPは一切変更していない。公開APIの
`display`・`keyboard`・`audio`だけを使っている。

エミュレーター上で確認できたもの:

- 10×20の井戸、7種類のピース、4方向の回転形状
- 落下、左右移動、回転（壁蹴り付き）、ソフトドロップ、ハードドロップ
- 着地判定、ライン検出、スコア表示（自作5×7フォント）
- ゲームオーバー判定と表示
- キー入力が実際にゲームを操作する

**実機は一度も使っていない。** ビルド → エミュレーター実行 → PNG確認、の反復だけで
書けた。これは`picocalc_emu`が目指した状態そのものである。

## 見つかった穴

> **その後（2026-08-05）:** 穴1・2・4はscenario runnerで解消した。詳細は
> [`SCENARIO_RUNNER.md`](SCENARIO_RUNNER.md)、証拠は
> `firmware-validation/records/milestone3-20260805-01/`。穴3については**host device modelと
> 専用`emu_smoke`の基盤が完成**した。ただしPicoTetris自身のライン消去・衝突判定を
> 呼び出すhost unit testはまだ無い。基盤完成とアプリ接続完了を区別し、後者は
> `MILESTONES.md`のR3で行う。各節の末尾に後続状態を追記した。

### 1. キー入力のタイミングを制御できない（最も重い）

`--keys "aaa "`は文字列を起動時に一括でFIFOへ積むだけで、**ゲームの状態を見て
次の操作を決められない**。結果として次ができない。

- 「ピースが着地したら次のキーを送る」
- 「この形が出たら左に3つ、それ以外は右に2つ」
- **ライン消去を意図的に起こす**

実際、ライン消去のロジックは書いたが**エミュレーター上で発火させられなかった**。
ハードドロップを連打すると全ピースが同じ列に積まれ、横移動を混ぜても列の割り当ては
運任せになる。コードの正しさは目視レビューに頼っている。

→ **Milestone 3のscenario runnerが必要**。キー投入を「いつ」「何を条件に」行うかを
記述できないと、条件付きの動作は検証できない。

> **解消（2026-08-05）。** `wait_until`が画面とUART出力を見てから次のキーを決める。
> `scenarios/tetris-line-clear.json`で**このライン消去を実際に発火させた**（13ライン、
> スコア1400）。配置は種を固定した乱数から事前に計算し、予測スコアと実行結果の
> 一致を`assert`している。
>
> なお実装中に、**1バーストで31イベント（15キー）を超えて積めない**ことが分かった。
> キーボードコントローラは深さを5ビットでしか報告できないためである。当時の
> エミュレーターはこの上限を持たず、滞留が224に達してBSPのドライバが恒久停止した。
> エミュレーター側の欠陥として修正済み。

### 2. 画面の検証が目視のみ

私がPNGを見て「グリッドが見える」「ピースが左に寄った」と判断している。
機械的な合否がない。次のような表明ができない。

- 「(3,19)のセルがマゼンタである」
- 「スコア領域が0以外を表示している」
- 「前フレームとの差分が井戸の内側だけである」

→ pixel/region assertionが要る。これもMilestone 3の範囲。

> **解消（2026-08-05）。** `pixel`・`region_non_black`・`region_hash`で機械判定できる。
> 実例として、井戸の6〜13行目の非黒画素数がちょうど4160＝「空セルの枠線だけ」で
> あることを`assert`し、積み上がりが計画どおり低いままであることを人間の目を
> 使わずに確認している。

### 3. アプリのロジックをホストで単体テストできない

ライン消去や衝突判定は純粋な関数だが、テストするにはRP2040バイナリを作って
エミュレーターで走らせるしかない。ホスト側でリンクして直接呼べれば、
`clear_lines()`に盤面を渡して結果を検査できる。

→ Milestone 2のhost backendの価値はここにある。当初「firmware backendがあるので
優先度が下がった」と評価したが、**アプリのロジック検証という別の用途がある**。

> **基盤を解消（2026-08-05）。** host backendがBSPの公開APIをホストのモデルに対して
> ビルドする。`python3 tools/picocalc.py test --mode host`で1秒未満で走る。
> 詳細は[`HOST_BACKEND.md`](HOST_BACKEND.md)。
>
> このコマンドが現在走らせるのは汎用`emu_smoke`であり、PicoTetrisの
> `clear_lines()`や`collides()`を直接試験しない。アプリ固有の穴はR3へ残る。
> ただし**firmware backendが権威である**ことは
> 変わらない。hostにはPIO・DMA・I2C・割り込みが無く、ハードウェアの挙動は
> 問いとして成立しない。両者を繋ぐのはframebuffer digestで、正規形が同一なので
> 同じ絵を描いたアプリは両方で同じhashを出す。

### 4. 途中経過が見えない

取得できるのは最終フレームのPNGだけである。「30秒時点でどうなっていたか」を
見るには、サイクル数を変えて実行し直すしかない。アニメーションや状態遷移を
追う用途には向かない。

→ 定期スナップショット、あるいは指定サイクルでのフレーム取得。

> **解消（2026-08-05）。** scenarioの`snapshot`操作が、走行中の任意の時点で
> PNGを書き、そのRGB565 hashをレポートへ記録する。等間隔にしたければ
> `wait`と`snapshot`を並べるだけでよい。

## 副次的な発見

- `--lcd-variant`の既定はB。A系統のファームウェアで既定のまま走らせると
  per-cycle GPIO観測が有効になり到達サイクルが約1/3になる（本文書とは別に
  `IMPLEMENTATION_STATUS.md`へ記録済み）。
- `constexpr`配列の範囲for文に`<initializer_list>`が要る。テンプレートは
  これをincludeしていないため、最初のビルドがエラーになった。テンプレート側で
  includeするか、READMEに書くかは判断が要る。

## 次に作るべきもの（この記録からの結論）

優先順位は上の穴の重さに従う。

1. ~~**scenario runner**（Milestone 3）~~ — **完了（2026-08-05）**。
   [`SCENARIO_RUNNER.md`](SCENARIO_RUNNER.md)
2. ~~**host backend**（Milestone 2）~~ — **完了（2026-08-05）**。
   [`HOST_BACKEND.md`](HOST_BACKEND.md)
3. ~~定期スナップショット~~ — **完了**。scenarioの`snapshot`操作に含まれる

**この時点で定義した基盤作業は完了した。** 実際にアプリを1本書いたことで、
計画からは出てこなかった要求が3つ出て、その基盤実装まで到達した。

> **後続レビュー（2026-08-05）:** PicoTetrisを継続回帰として運用するには、生成契約と
> provenance、アプリ固有host unit test、再現可能BIN、target registry/CLI、CIがさらに必要と
> 判明した。これは当時の観察を取り消すものではなく、基盤を実運用へ接続する次段階である。
> 順序と受入条件は`MILESTONES.md`のR0〜R4を正典とする。

## 成果物

ゲームのソースは`~/pico_dvl/codex/picotetris`に独立リポジトリとして置いた
（commit `51d0191`）。当時は`build/tetris/`の作業ツリーにしかなかったものを、
420行を捨てないために版管理下へ移した。BSPは生成時のコピーのままで、
変更していない。

このゲームはエミュレーターの**回帰候補かつ現在のscenario証拠**である。
`scenarios/tetris-line-clear.json`が実行するのはこのバイナリであり、
そこで見つかったキーボードモデルの欠陥は、実際のアプリを走らせなければ
出てこなかった種類のものである。source identity、再現可能build、target登録、CIまでを
満たす正式な継続回帰化はR3/R4で行う。
