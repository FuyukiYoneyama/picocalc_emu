{
  "schema_version": 2,
  "backend": {
    "repo": "picoem-picocalc",
    "url": "https://github.com/FuyukiYoneyama/picoem-picocalc",
    "branch": "main",
    "execution_model": "Serial",
    "roles": {
      "hardware_correlated": {
        "commit": "612b48510452d4012e4ac6639960ca3983b48f66",
        "target": "picotetris-r5",
        "status": "immutable_evidence",
        "evidence": "firmware-validation/records/r5-hardware-20260808-01/record.json",
        "meaning": "The backend used for the immutable same-artifact R5 emulator preflight whose UF2 passed on physical PicoCalc hardware."
      },
      "promoted": {
        "commit": "e985a9d7ecb51ef760506a105edd34e31cf9b5f1",
        "target": "picotetris-opt1b",
        "status": "active",
        "validation": "firmware-validation/validations/picotetris-opt1b-r5.json",
        "meaning": "The current accepted backend for normal regression. It preserves the R5 behavior contract and adds the promoted OPT1-B serial fast path."
      },
      "experimental_main": {
        "commit": "38683d65800ef36026f674dd47228024d69eb5e7",
        "status": "unpromoted",
        "promoted": false,
        "ci_run_id": 31307835028,
        "meaning": "Observed main after NEXT-2A added core-local Serial SIO FIFO IRQ projection and core 1 fatal-exception detection. The bounded multicore behavior is accepted by its own active target, but this commit does not replace the general promoted PicoTetris backend merely because it is branch head."
      }
    },
    "note": "Source is never vendored here. Historical targets retain exact backend pins in the target registry. The three roles deliberately separate physical-device correlation, current promotion and an unpromoted moving development head."
  },
  "scope": {
    "authority": "firmware-emulator-rp2040-binary-conformance",
    "hardware_or_bsp_audio_evidence_implies_emulator_audio_output": false,
    "note": "R5 audio=pass covers firmware configuration/counters plus physical reference-tone evidence. It does not mean that the emulator produces a PCM waveform; audio-output remains unsupported below."
  },
  "supported": [
    {
      "id": "direct-boot",
      "what": "Direct boot of a Pico SDK image from flash offset 0x100",
      "detail": "The bootrom image is loaded so ROM function lookups resolve, but never executed; SP/PC/VTOR are seeded from the SDK vector table. Images without an SDK vector table fall back to the bootrom reset vector and are reported as boot.mode.",
      "since_gate": 1
    },
    {
      "id": "xip-read",
      "what": "Instruction fetch and literal reads from XIP flash at 0x1000_0000",
      "since_gate": 1
    },
    {
      "id": "uart0-capture",
      "what": "UART0 transmit capture as raw bytes plus a SHA-256",
      "since_gate": 1
    },
    {
      "id": "unsupported-mmio-log",
      "what": "Opt-in log of accesses to undecoded MMIO addresses, with the accessing PC",
      "limitation": "Only undecoded addresses fault and are recorded. A peripheral that is decoded but partially implemented does not appear here.",
      "since_gate": 1
    },
    {
      "id": "spi-external-device",
      "what": "Generic off-chip SPI slave hook on either controller",
      "detail": "Words are handed to the device as they drain from the TX FIFO, with pad levels presented immediately beforehand so CS/DC framing is correct at any step quantum.",
      "since_gate": 2
    },
    {
      "id": "lcd-st7365p",
      "what": "ST7365P display model on SPI1 with GP13/14/15 as CS/DC/RESET",
      "detail": "320x480 RGB565 frame memory, CASET/RASET windowing, RAMWR in RGB565 or RGB666, RAMRD readback, and a 320x320 viewport exported as a canonical RGB565 SHA-256 and a deterministic PNG.",
      "limitation": "MADCTL and INVON are recorded but not applied to the logical framebuffer; both are panel-wiring compensations that would invert or mirror the image relative to what a person sees on the device.",
      "since_gate": 2
    },
    {
      "id": "pio",
      "what": "Both PIO blocks, advanced one system cycle at a time whenever a pin-watching device is attached",
      "detail": "Bulk advancement would drop the SCK edges of a PIO-driven SPI program between quantum boundaries.",
      "since_gate": 3
    },
    {
      "id": "dma",
      "what": "12-channel DMA including the DREQ matrix, used here to feed the PIO TX FIFO",
      "since_gate": 3
    },
    {
      "id": "psram",
      "what": "APS6404L 8 MiB SPI PSRAM on PIO1 with CS20/SCK21/MOSI2/MISO3",
      "detail": "Reset-enable, reset, write, fast-read and Read ID. The read output is held one SCK to match the real chip's settling time, applied to both fast-read and Read ID because it is a property of the output driver rather than of the command. The reported identity is the value the PicoCalc's own chip returned during hardware correlation.",
      "since_gate": 3,
      "correlated": "2026-08-05 against hardware: verify passes and the ID matches (hardware-validation/records/bsp-0.8.8-20260804-02.json)."
    },
    {
      "id": "i2c-external-device",
      "what": "Generic off-chip I2C slave hook on either controller",
      "detail": "A device claims its own address; with none attached the historical always-ACK stub list still applies.",
      "since_gate": 4
    },
    {
      "id": "keyboard-controller",
      "what": "PicoCalc STM32 keyboard/power controller at address 0x1F on I2C1",
      "detail": "ClockworkPi's official PicoCalc/Code/picocalc_keyboard STM32F103R8T6 firmware is the primary behavioral reference. The model covers the consumer-visible 0x01..0x0E register replies, 31-event FIFO, lock flags, LCD/keyboard backlight quantisation, battery, reset, C64 matrix/joystick and power-off request. It also pins the official 7x8 matrix plus 12-button map, modifier codes, Caps/Num behavior, state encoding, and strict hold/repeat thresholds. Register selection survives STOP, and a new read request resends the latched reply from byte zero as the controller does.",
      "limitation": "The model accepts physical matrix/button transitions through a conformance API but does not simulate GPIO electrical scanning, debounce, 16 ms polling, or PMU-driven battery/power-key lifecycle. Scenario key steps remain logical FIFO injection for backward compatibility. Bus speed is not modelled, so 10 kHz and 400 kHz behave identically. CFG/INT/DEB/FRQ stay internal because the current official receiveEvent switch does not expose them over I2C.",
      "since_gate": 4
    },
    {
      "id": "pwm-observation",
      "what": "PWM slice configuration is readable for reporting",
      "limitation": "Configuration only. No audio is produced and DMA-paced playback is not exercised.",
      "since_gate": 5
    },
    {
      "id": "wfe-irq-wake",
      "what": "A core parked on WFE wakes on an enabled pending interrupt, per ARMv6-M B1.5.18",
      "detail": "Required by the Pico SDK sleep idiom; without it an alarm-plus-WFE loop live-locks.",
      "since_gate": 2
    },
    {
      "id": "flash-chip-commands",
      "what": "SPI flash behind XIP_SSI answers the boot-time commands firmware asks the chip",
      "detail": "Read Unique ID (0x4B), JEDEC ID (0x9F) and the two status registers. The FIFO level registers (TXFLR/RXFLR) track the reply queue, because the bootrom transfer loop reads those rather than the status flags. Required by pico_unique_id, which the standard template links and the official sample does not.",
      "limitation": "Erase and program are deliberately absent: nothing in the conformance track writes flash, and a half-modelled program path would corrupt the XIP image rather than fail visibly.",
      "since_gate": 7
    },
    {
      "id": "lcd-variant-b",
      "what": "The Canonical BSP default display path: PIO0, RGB565, LCD DMA off",
      "detail": "A pin-watching wire reassembles bytes from the PIO0 shift program's SCK edges, tracks the CPU-driven CS/DC/RESET lines, and drives MISO so the SIO bit-bang GRAM readback works. Feeds the same ST7365P model as variant A; the transfer paths stay separate.",
      "since_gate": 7
    },
    {
      "id": "pio-txstall",
      "what": "FDEBUG.TXSTALL latches when a state machine runs dry on an empty TX FIFO",
      "detail": "The end-of-transfer signal firmware actually waits on. FSTAT's TXEMPTY cannot serve, because it goes true while the last word is still being shifted out.",
      "since_gate": 7
    },
    {
      "id": "sd-card",
      "what": "SD card in SPI mode on SPI0, with card detect on GP22",
      "detail": "SPI bring-up as the BSP drives it (CMD0, CMD8, CMD55+ACMD41 answering busy a couple of times before ready, CMD58 reporting high capacity), plus single-block read and write. The generated test card is 64 MiB and defaults to FAT32 to match the filesystem choice of PicoCalc's bundled 32 GB card; FAT16 is an explicit compatibility profile. Both complete mount, write, sync, read, compare and remove through the BSP's Chan FatFs. Runner schema 8 retains the selected format and block counters introduced in schema 6.",
      "limitation": "There is no multi-block transfer, CSD/CID beyond bring-up, removal, write-protect, raw-image persistence or directory-backed storage, and writes complete immediately rather than holding the host through a programming delay.",
      "correlated": "2026-08-09: BSP 0.9.0 public filesystem write path passed same-artifact PicoEdit correlation for FAT32 load, create/truncate write, sync, rename/backup and readback (firmware-validation/records/next1-picoedit-hardware-20260809-01/record.json). FAT16 remains host-only compatibility evidence.",
      "since_gate": "post-7; fat32-default completed in R1-SD"
    },
    {
      "id": "scenario-runner",
      "what": "JSON scenarios evaluated inside the run loop: timed and raw keyboard input, and pixel / region / UART assertions",
      "detail": "Operations are wait, wait_cycles, wait_until, key, key_events, snapshot and assert; conditions are pixel, region_non_black, region_hash, region_stable, region_changed and uart_contains. `key` supplies a normal press/release pair, while `key_events` preserves an explicit pressed/held/released sequence and raw official key code for keyboard-firmware conformance scenarios. A step can therefore choose the next key from what the program is currently showing, which --keys cannot. Milliseconds are virtual, taken from emulated cycles over the system clock the firmware has programmed, and re-based whenever clk_sys changes. Exit code 1 means a step failed; 2 still means the run could not be judged.",
      "limitation": "No loop or branch construct -- repetition has to be unrolled by whatever generates the scenario. Conditions are sampled at poll_ms (default 5 ms), so a state that appears and disappears inside one tick is missed; timed waits themselves are exact. Scenarios cannot assert on report fields such as key_events_dropped.",
      "since_gate": "milestone-3"
    },
    {
      "id": "snapshot-mid-run",
      "what": "Framebuffer PNG and RGB565 digest captured at any point during a run, not only at the end",
      "detail": "A snapshot step writes to --snapshot-dir and records the digest in the scenario section. Only basenames reach the report.",
      "since_gate": "milestone-3"
    },
    {
      "id": "keyboard-fifo-depth",
      "what": "The keyboard controller's queue is bounded at 31 events, the deepest its count register can express",
      "detail": "The official STM32 firmware defines FIFO_SIZE=31 and KEY_COUNT_MASK=0x1F; the Canonical BSP reads the same field as `key_info[0] & 0x1f`. The official default leaves CFG_OVERFLOW_ON clear and drops the arriving event. The model also covers the internal CFG_OVERFLOW_ON alternative, which overwrites the oldest event while keeping count 31. Loss, overwrite and the overflow interrupt latch are reported; schema 8 makes any lost event a failed verdict.",
      "limitation": "The current official receiveEvent switch has no CFG register case, so the alternative policy is reachable only through a conformance-test internal setter, not a fictitious I2C write.",
      "since_gate": "milestone-3"
    },
    {
      "id": "firmware-verdict",
      "what": "Fail-closed firmware verdict shared by the structured report and process exit code",
      "detail": "Runner schema 8 records verdict.status, stable reason codes, the accepted stop reason, required UART markers, missing markers, and the backend identity compiled into the runner. Exit 0 is pass, 1 is a judged failure, and 2 is cannot_judge. Exception, emulator error, unsupported or truncated MMIO, keyboard drop, unknown keyboard register access, scenario assertion/timeout/incomplete, stop mismatch, marker absence, stale backend build, and dirty backend build cannot pass. A raw cycle limit without an accepted stop is cannot_judge; a scenario infrastructure fault is also cannot_judge.",
      "since_gate": "R1-verdict"
    },
    {
      "id": "firmware-target-contract",
      "what": "A schema-validated one-command contract for each registered firmware target",
      "detail": "R2 registry schema 2 bound source and toolchain identity, exact BIN and optional scenario SHA-256, compiled backend commit, device profile, accepted stop, required UART markers, and dotted structured-report checks. R4 schema 3 adds immutable target revisions, supersedes links, and SHA-pinned validation attestations without rewriting historical evidence. picocalc.py creates a fresh report for every run, forwards every declared runner option, rejects conflicting CLI overrides before execution, and preserves the runner's 0/1/2 verdict classes.",
      "since_gate": "R2"
    },
    {
      "id": "picotetris-r3-regression",
      "what": "Formal PicoTetris application regression across native game logic and the RP2040 firmware path",
      "detail": "The game rules are hardware-free and covered by 666 native checks. Historical target picotetris-r3 pins the original source/BSP/toolchain/BIN/backend/FAT32/scenario/report expectations; picotetris-r4 revision 2 preserves the same firmware artifact and validates it against the R4 backend commit. Two fresh builds produced identical BIN and UF2 payloads, three recorded firmware runs produced identical UART, RGB565 framebuffer, PNG, raw and normalized reports, and 85-step timelines while clearing 13 lines for score 1400 with 362 delivered and zero dropped key events, and the three repositories now run independent clean-runner CI gates.",
      "limitation": "R3/R4 are emulator and CI evidence. Correlation of the exact registered BIN SHA on a physical PicoCalc remains R5.",
      "since_gate": "R3"
    },
    {
      "id": "picotetris-r5-correlation",
      "what": "One reproducible PicoTetris R5 firmware artifact for emulator preflight and physical PicoCalc correlation",
      "detail": "Target picotetris-r5 revision 4 pins source 9a40a905, backend 612b485, one BIN/UF2 pair, FAT32, and the official ClockworkPi keyboard source commit. The firmware automatically checks 100 LCD readbacks, audio stream counters, PSRAM, SD smoke, deterministic line clear, game over and restart, then accepts all 67 physical keys in arbitrary order. Individual keys can be retried without timeout; checksum-protected SD progress resumes after power loss. Host coverage is 690 checks. Emulator preflight passed 5/5 steps with 136 raw events, 67/67 keys and no drops. The same UF2 passed on CPI2.0/RP2040 hardware with the exact final verdict, final PASS photograph, acoustic reference-tone evidence, and CRC-valid SD progress fixed in r5-hardware-20260808-01.",
      "limitation": "Keyboard 67/67 proves eventual press/release reachability, not missed physical presses, actuation force or response latency. The operator observed that some keys felt less responsive; input-quality measurement remains separate from protocol conformance.",
      "since_gate": "R5"
    },
    {
      "id": "exact-idle-fast-forward",
      "what": "Exact event-horizon fast-forward while both RP2040 cores are blocked",
      "detail": "Target picotetris-opt1a revision 3 preserves the registered PicoTetris firmware, scenario, 927528660 cycles, UART, RGB565 framebuffer and 85-step timeline on backend c68c58f6c37fb31eb9313566c8b16883db9063b6. Behavior trace schema 2 separates UART guest-byte events from host diagnostic draining and preserves all nine event-domain counts and hashes against a one-cycle reference. Ten trace-off runs reduced median wall time from 63.2470 s to 27.1229 s, a 2.3319x speedup, while deterministic report, UART and PNG artifacts remained identical. R5 then correlated the same registered firmware family on physical PicoCalc hardware, so OPT1-A is promoted.",
      "limitation": "Promotion applies to the exactness and workloads covered by OPT1-A and R5; later batching or CPU optimizations require their own correctness, determinism, performance and hardware-correlation decisions.",
      "since_gate": "R5"
    },
    {
      "id": "serial-fast-path-gate",
      "what": "Short-circuit read-only Serial fast-path predicates after active PIO has already selected the slow path",
      "detail": "Target picotetris-opt1b revision 5 pins backend e985a9d7ecb51ef760506a105edd34e31cf9b5f1. Against the promoted OPT1-A/R5 backend, PicoTetris preserves 927528660 cycles, the 85-step timeline, UART, framebuffer, behavior SHA-256, 173498680 streaming events and every count/hash in all nine domains. Ten trace-off runs reduced median wall time from 27.122874 s to 25.381594 s (6.419972%) and raised real-time ratio to 14.636593%. Template B stayed within the 3% non-regression gate, official Hello passed an 8 MiB PSRAM verification, and the R5 correlation firmware reproduced the immutable emulator preflight except for backend identity. Existing physical evidence therefore transfers through exact equivalence and OPT1-B is promoted.",
      "limitation": "The optimization only changes predicate evaluation order and removes a duplicate DMA idle query. OPT2 event-batching candidates were exact where claimed but failed the performance gate and were reverted. OPT3-B's short immutable-XIP decode cursor preserved exactness but regressed median wall time by 4.4265%; OPT3-C's compact dispatch key preserved exactness but improved by only 4.1542%, below the 5% gate. Both were reverted, no additional optimization was promoted, and performance work is paused while scope-expansion work proceeds.",
      "since_gate": "OPT1-B"
    },
    {
      "id": "host-backend",
      "what": "The BSP's public API built against host models, so application logic runs natively",
      "detail": "Lives in picocalc_emu under bsp/host and does not use this emulator. display is a framebuffer, keyboard a bounded queue, psram host memory, sdcard an array of sectors; the filesystem layer and ChanFatFS are the device's own sources compiled unchanged on top of the RAM block device. sleep_ms advances a virtual clock rather than blocking. Run with `picocalc.py test --mode host`; bsp/host/tests/emu_smoke.cpp is Milestone 2's completion condition and its output is byte-identical across runs.",
      "limitation": "Not a model of the chip: no PIO, DMA, I2C transaction, interrupt or multicore, and no LCD wire format, so verify_pixels always reports transport_ok. Directory-backed Fast SD mode is not implemented. audio underrun_count is always zero because nothing consumes samples on a schedule -- that is the absence of a measurement, not a result. This backend answers whether the application computed the right thing; the firmware backend remains the authority on hardware behaviour.",
      "since_gate": "milestone-2"
    },
    {
      "id": "cross-backend-framebuffer-digest",
      "what": "The host and firmware backends hash the framebuffer identically",
      "detail": "Both define the canonical form as the raw RGB565 byte stream -- width * height * 2 bytes, row-major, little-endian per pixel -- so an application that draws the same picture under both produces the same digest in picocalc-run's framebuffer.rgb565_sha256 and in picocalc::host::framebuffer_sha256(). This is what lets a cheap host run be checked against a slow firmware one. Hashing a PNG instead would break the moment either encoder changed, without the picture changing.",
      "since_gate": "milestone-2"
    },
    {
      "id": "multicore-serial-conformance",
      "what": "Firmware launch of core 1, bidirectional SIO FIFO, WFE/SEV and core-local SIO IRQ delivery under the Serial execution model",
      "detail": "Active target picocalc-multicore-r2 pins application e9e99f0bfde7b2706fbe7f5a2a92331eed141c98 and backend 38683d65800ef36026f674dd47228024d69eb5e7. Three normal CLI runs are byte-identical in report, UART, scenario timeline and framebuffer snapshot; two clean clones reproduce the same BIN and UF2. The harness also fails closed on a core 1 NMI or HardFault. R2 preserves the accepted phase behavior and periodically repeats its final marker block so PicoCalc USB CDC can be attached after boot without losing the evidence. On 2026-08-09 the same registered v2 UF2 passed physical-device correlation with 72 complete USB marker blocks and a final photograph showing all five PASS results.",
      "limitation": "This proves the frozen NEXT-2A Serial workload only. Threaded execution, simultaneous LCD/PSRAM access from both cores, spinlock contention timing, core 1 reset/relaunch and DMA-paced PCM are not covered. The separate v1 one-shot capture remains preserved as incomplete historical evidence and is not silently upgraded by the v2 result.",
      "target": "picocalc-multicore-r2",
      "emulator_evidence": "firmware-validation/records/next2-multicore-r2-20260809-01/record.json",
      "evidence": "firmware-validation/records/next2-multicore-r2-hardware-20260809-01/record.json",
      "since_gate": "NEXT-2A"
    }
  ],
  "unsupported": [
    {
      "id": "audio-output",
      "what": "PWM audio playback through DMA to a sample sink",
      "detail": "PWM configuration is observable; sample streaming is not modelled."
    },
    {
      "id": "bootrom-execution",
      "what": "Executing the real bootrom, including USB MSC boot",
      "detail": "QSPI pads are not modelled, so boot uses the direct-boot shortcut. The SSI block itself now models flash command and response (see flash-chip-commands); what is absent is running the bootrom's own boot path."
    },
    {
      "id": "threaded-model",
      "what": "The threaded execution model as a correctness reference",
      "detail": "Serial is the reference. The threaded worker loop still has the sub-quantum GPIO-edge gap and the WFE interrupt-wake gap that were fixed on the serial path."
    },
    {
      "id": "timing-fidelity",
      "what": "Cycle-accurate agreement with silicon",
      "detail": "Peripheral latencies are modelled where they change observable ordering, not calibrated against real hardware. A first correlation run on 2026-08-05 (hardware-validation/records/bsp-0.8.8-20260804-02.json) found agreement on boot, clock, LCD verification and audio health, and one disagreement on PSRAM."
    }
  ]
}
